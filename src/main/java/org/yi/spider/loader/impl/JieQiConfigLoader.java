package org.yi.spider.loader.impl;

import org.yi.spider.loader.ILoader;

public class JieQiConfigLoader implements ILoader {
	
//	private static final Logger logger = LoggerFactory.getLogger(JieQiConfigLoader.class);

	@Override
	public void load() {
		/*INovelService novelService = new ServiceFactory().createNovelService(ProgramEnum.JIEQI.getName());
		try {
			Map<String, Object> map = novelService.loadSystemParam();
			GlobalConfig.localSite.setKeywords(String.valueOf(map.get("")));
		} catch (SQLException e) {
			logger.error(e.getMessage(), e);
		}*/
	}
	
	
}
